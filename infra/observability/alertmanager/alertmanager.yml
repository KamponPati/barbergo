global:
  resolve_timeout: 5m

route:
  group_by: ["alertname", "service", "severity"]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
  receiver: default-log

receivers:
  - name: default-log
    # Local baseline receiver (intentionally empty sink in staging).
    # Replace with Slack/Email/PagerDuty in production.
