[Unit]
Description=BarberGo Backup (DB + Object Storage)
After=network.target docker.service
Wants=docker.service

[Service]
Type=oneshot
User=root
Group=root
WorkingDirectory=/home/yee/app
Environment=OUT_DIR=/var/backups/barbergo
ExecStart=/usr/bin/env bash -lc 'bash scripts/backup/run-backup.sh'
